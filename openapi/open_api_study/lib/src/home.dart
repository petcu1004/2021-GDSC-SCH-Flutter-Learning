import 'package:flutter/material.dart';
import 'package:open_api_study/src/map/map_marker.dart';
import 'package:open_api_study/src/ui/list.dart';

class Home extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body:
          // MapSample(),
          ListWidget(),
    );
  }
}
